<div>
    <strong class="notifications relative cursor-pointer"><i class="fa-bell text-24 text-green-4e8892"></i>
        @if($notificationCount > 0)
            <span class="absolute text-white bg-red-ce1400 rounded-full text-11 font-bold text-center">{{ $notificationCount }}</span>
        @endif
    </strong>
</div>
